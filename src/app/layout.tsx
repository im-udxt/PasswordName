import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "The Ultimate Password Challenge",
  description: "The Ultimate Password Challenge is an engaging and fiendishly difficult web game that puts a unique twist on password creation. Created by Team Error404, this game presents players with 25 increasingly complex rules that their password must satisfy simultaneously. Starting with basic requirements like length and uppercase letters, it quickly escalates to demanding elements like prime numbers, chemical symbols, chess moves, and even binary ASCII representations!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
